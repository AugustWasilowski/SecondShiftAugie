# Single requirements list for the container's one venv. Voice helper +
# augie-speak MCP server share it. No ML deps; this is intentionally
# lean — the brain is Claude Code (a separate process), and the voice
# pipeline is just `Piper WAV → ffmpeg → Discord voice gateway`.

# Voice helper
nextcord==2.6.0
aiohttp==3.9.5
python-dotenv==1.0.1
PyNaCl==1.5.0

# augie-speak MCP server (stdio bridge from Claude Code to the voice
# helper's loopback HTTP API)
mcp>=1.2.0
